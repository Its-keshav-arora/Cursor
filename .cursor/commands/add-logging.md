import the logging library
create a logger object
add a log line at the start of every function with relevant context
add error logging in every exception block
and I include a sample snippet as a reference